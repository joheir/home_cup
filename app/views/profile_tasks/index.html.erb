<div class="wrapper">
  <div class="header">
    <p>Make a text that updates</p>
    <p>The progress<%= @profile.progress %></p>


  </div>
  <div class="task-overview">
    <ul>

      <% @profile_tasks.each do |profile_task| %>
        <li>
          <%= profile_task.task_template.title %>
          <%= profile_task.task_template.description %>
          <%= button_to profile_profile_task_path(profile_task.profile, profile_task), method: :put, class: "check-box-buton" do %>
            <% if profile_task.done? %>
              <i class="fa-solid fa-square"></i>
              <%= profile_task.done %>
            <% else %>
              <i class="fa-regular fa-square"></i>
              <%= profile_task.done %>
            <% end %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
