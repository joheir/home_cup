<%= simple_form_for(@profile, html: {"data-turbo": false}) do |f| %>
  <%= f.input :name %>
  <%= f.input :admin, as: :boolean %>
  <%= f.input :age %>
  <%= f.input :picture, as: :file %>
  <%= f.submit %>
<% end %>
