<%= simple_form_for(@task_template, html: {"data-turbo": false} ) do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :min_age %>
    <%= f.input :weekday %>


    <div class="form-actions">
      <%= f.button :submit, 'Submit!' %>
    </div>
  <% end %>
